{
  "name": "@jue/tools",
  "version": "0.1.0",
  "description": "工具子系统:注册 / 执行 / 适配",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": ["dist", "src"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -p tsconfig.json --watch",
    "clean": "rimraf dist",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "@jue/shared-types": "workspace:*",
    "@jue/utils": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^6.0.3"
  }
}
